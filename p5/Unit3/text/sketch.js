function setup() {
  createCanvas(500, 500);
  textAlign(CENTER);
  rectMode(CENTER);
}

function draw() {
  background(100);
  textSize(60);
  textStyle(BOLDITALTC);
  textLeading(60);
  text('PLEASE WEAR A MASK!!! NOTE: AND WASH YOUR HAND', width/2, height/2)

}
